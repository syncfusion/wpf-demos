<Window x:Class="ObservableCollectionDataCharts.PointPropertiesWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Title="Data properties Window" SizeToContent="WidthAndHeight"
    WindowStartupLocation="CenterScreen" VerticalContentAlignment="Center"
        HorizontalAlignment="Center" ResizeMode="NoResize"
        ShowInTaskbar="False">
  <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Margin="20">
    <!--<StackPanel.Resources>
      <Style TargetType="Label">
        <Setter Property="VerticalAlignment" Value="Center"/>
      </Style>
    </StackPanel.Resources>
    <StackPanel Orientation="Horizontal">
      <Label FontSize="14">Item: </Label>
            <Label FontSize="14" Content="{Binding SportID, Mode=TwoWay}"/>
    </StackPanel>
    <StackPanel Orientation="Horizontal">
      <Label>SportName</Label>
      <TextBox Text="{Binding SportName,Mode=TwoWay}" Width="150" Name="xValue"/>
      <Label>Interest</Label>
      <TextBox Text="{Binding Interest,Mode=TwoWay}" Width="50" Name="yValue"/>
    </StackPanel>
    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
      <Button HorizontalAlignment="Center" 
            Padding="10,3,10,3"
            Margin="10"
            Content="OK"
              x:Name="OkButton"
            IsDefault="True"
            IsCancel="False"  Command="{Binding OkCommand}" CommandParameter="{Binding RelativeSource={RelativeSource Mode=FindAncestor, AncestorType={x:Type Window}}}" />
    </StackPanel>-->
  </StackPanel>
</Window>
