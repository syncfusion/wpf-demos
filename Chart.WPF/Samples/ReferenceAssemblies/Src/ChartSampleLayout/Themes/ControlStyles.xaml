<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:syncfusion="http://schemas.syncfusion.com/wpf"
                    >
        <ResourceDictionary.MergedDictionaries>
            <ResourceDictionary Source="/syncfusion.Shared.WPF;component/SkinManager/MetroStyle.xaml" />
            <ResourceDictionary Source="/syncfusion.Shared.WPF;component/Controls/ColorPicker/Themes/MetroStyle.xaml" />
        </ResourceDictionary.MergedDictionaries>
        
    <Style TargetType="{x:Type ComboBox}" BasedOn="{StaticResource MetroComboBoxStyle}">
            <Setter Property="Margin" Value="10"/>
            <Setter Property="Height" Value="30"/>
            <Setter Property="IsEditable" Value="True"/>
            <Setter Property="TextBlock.TextWrapping" Value="NoWrap"/>
        
        
    </Style>
    
    <Style TargetType="{x:Type CheckBox}" BasedOn="{StaticResource MetroCheckBoxStyle}">
            <Setter Property="Margin" Value="10"/>
        <Setter Property="HorizontalAlignment" Value="Left"/>
        </Style>
    
    <Style TargetType="{x:Type TextBox}" BasedOn="{StaticResource MetroTextBoxStyle}">
            <Setter Property="Margin" Value="10"/>
            <Setter Property="TextBlock.TextWrapping" Value="Wrap"/>
        <Setter Property="HorizontalAlignment" Value="Stretch"/>
        </Style>

    <Style TargetType="{x:Type TextBlock}" BasedOn="{StaticResource MetroTextBlockStyle}">
            <Setter Property="TextBlock.TextWrapping" Value="Wrap"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        <Setter Property="HorizontalAlignment" Value="Left"/>
        <Setter Property="Margin" Value="10,0,0,0"/>
        </Style>
    
    <Style TargetType="{x:Type Button}" BasedOn="{StaticResource MetroButtonStyle}">
            <Setter Property="Margin" Value="10"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="MinWidth" Value="100"/>
        </Style>

    <Style TargetType="{x:Type RadioButton}" BasedOn="{StaticResource MetroRadioButtonStyle}">
        <Setter Property="Margin" Value="10"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
        <Setter Property="Height" Value="20"/>
        <Setter Property="HorizontalAlignment" Value="Left"/>
    </Style>

    <Style TargetType="{x:Type Slider}" BasedOn="{StaticResource MetroThemeSliderStyle}">
        <Setter Property="Margin" Value="10"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
        <Setter Property="Height" Value="20"/>
        <Setter Property="HorizontalAlignment" Value="Stretch"/>
    </Style>

    <Style TargetType="{x:Type Expander}" BasedOn="{StaticResource MetroExpanderStyle}">
        <Setter Property="Margin" Value="5,10,5,10"/>
        <Setter Property="Padding" Value="0,0,0,5"/>
        <Setter Property="BorderThickness" Value="0,0,0,1"/>
        <Setter Property="HeaderTemplate">
            <Setter.Value>
                <DataTemplate>
                    <TextBlock Text="{Binding}" FontWeight="SemiBold" />
                </DataTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="{x:Type syncfusion:ColorPicker}" BasedOn="{StaticResource MetroColorPickerStyle}">
            <Setter Property="Margin" Value="10"/>
        <Setter Property="HorizontalAlignment" Value="Stretch"/>
    </Style>


</ResourceDictionary>