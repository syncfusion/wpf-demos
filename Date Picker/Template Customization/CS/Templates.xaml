<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <DataTemplate x:Key="DayCellTemplate1">
        <Grid>
            <Image Source="/TemplateCustomization;component/Assets/sun.png" VerticalAlignment="Top" HorizontalAlignment="Right" Margin="5"
                       Stretch="Uniform" Width="16"/>
            <StackPanel HorizontalAlignment="Left" Margin="2" VerticalAlignment="Bottom">
                <TextBlock  FontSize="28" Margin="0 0 0 -3" Text="{Binding DayNumber}"/>
                <TextBlock FontSize="12" FontWeight="Light" Text="{Binding DayName}"/>
            </StackPanel>
        </Grid>
    </DataTemplate>
    <DataTemplate x:Key="DayCellTemplate2">
        <Grid>
            <Image Source="/TemplateCustomization;component/Assets/smile.png" VerticalAlignment="Top" HorizontalAlignment="Right" Margin="5"
                       Stretch="Uniform" Width="16"/>
            <StackPanel HorizontalAlignment="Left" Margin="2" VerticalAlignment="Bottom">
                <TextBlock  FontSize="28" Margin="0 0 0 -3" Text="{Binding DayNumber}"/>
                <TextBlock FontSize="12" FontWeight="Light" Text="{Binding DayName}"/>
            </StackPanel>
        </Grid>
    </DataTemplate>
    <DataTemplate x:Key="DayCellTemplate3">
        <Grid>
            <Image Source="/TemplateCustomization;component/Assets/graduation.png" VerticalAlignment="Top" HorizontalAlignment="Right" Margin="5"
                       Stretch="Uniform" Width="16"/>
            <StackPanel HorizontalAlignment="Left" Margin="2" VerticalAlignment="Bottom">
                <TextBlock  FontSize="28" Margin="0 0 0 -3" Text="{Binding DayNumber}"/>
                <TextBlock FontSize="12" FontWeight="Light" Text="{Binding DayName}"/>
            </StackPanel>
        </Grid>
    </DataTemplate>
    <DataTemplate x:Key="DayCellTemplate4">
        <Grid>
            <Image Source="/TemplateCustomization;component/Assets/rain.png" VerticalAlignment="Top" HorizontalAlignment="Right" Margin="5"
                       Stretch="Uniform" Width="16"/>
            <StackPanel HorizontalAlignment="Left" Margin="2" VerticalAlignment="Bottom">
                <TextBlock  FontSize="28" Margin="0 0 0 -3" Text="{Binding DayNumber}"/>
                <TextBlock FontSize="12" FontWeight="Light" Text="{Binding DayName}"/>
            </StackPanel>
        </Grid>
    </DataTemplate>
    <DataTemplate x:Key="DayCellTemplate5">
        <Grid>
            <Image Source="/TemplateCustomization;component/Assets/gift.png" VerticalAlignment="Top" HorizontalAlignment="Right" Margin="5"
                       Stretch="Uniform" Width="16"/>
            <StackPanel HorizontalAlignment="Left" Margin="2" VerticalAlignment="Bottom">
                <TextBlock  FontSize="28" Margin="0 0 0 -3" Text="{Binding DayNumber}"/>
                <TextBlock FontSize="12" FontWeight="Light" Text="{Binding DayName}"/>
            </StackPanel>
        </Grid>
    </DataTemplate>
    <DataTemplate x:Key="DayCellTemplate6">
        <Grid>
            <Image Source="/TemplateCustomization;component/Assets/graduation.png" VerticalAlignment="Top" HorizontalAlignment="Right" Margin="5"
                       Stretch="Uniform" Width="16"/>
            <StackPanel HorizontalAlignment="Left" Margin="2" VerticalAlignment="Bottom">
                <TextBlock  FontSize="28" Margin="0 0 0 -3" Text="{Binding DayNumber}"/>
                <TextBlock FontSize="12" FontWeight="Light" Text="{Binding DayName}"/>
            </StackPanel>
        </Grid>
    </DataTemplate>
    <DataTemplate x:Key="DayCellTemplate0">
        <Grid>
            <Image Source="/TemplateCustomization;component/Assets/medal.png" VerticalAlignment="Top" HorizontalAlignment="Right" Margin="5"
                       Stretch="Uniform" Width="16"/>
            <StackPanel HorizontalAlignment="Left" Margin="2" VerticalAlignment="Bottom">
                <TextBlock  FontSize="28" Margin="0 0 0 -3" Text="{Binding DayNumber}"/>
                <TextBlock FontSize="12" FontWeight="Light" Text="{Binding DayName}"/>
            </StackPanel>
        </Grid>
    </DataTemplate>
    <DataTemplate x:Key="MonthCellTemplate0">
        <Grid>
            <Image Source="/TemplateCustomization;component/Assets/love.png" VerticalAlignment="Top" HorizontalAlignment="Right" Margin="5"
                       Stretch="Uniform" Width="16"/>
            <StackPanel HorizontalAlignment="Left" Margin="2" VerticalAlignment="Bottom">
                <TextBlock  FontSize="28" Margin="0 0 0 -3" Text="{Binding MonthNumber}"/>
                <TextBlock FontSize="12" FontWeight="Light" Text="{Binding MonthName}"/>
            </StackPanel>
        </Grid>
    </DataTemplate>
    <DataTemplate x:Key="MonthCellTemplate1">
        <Grid>
            <Image Source="/TemplateCustomization;component/Assets/ball.png" VerticalAlignment="Top" HorizontalAlignment="Right" Margin="5"
                       Stretch="Uniform" Width="16"/>
            <StackPanel HorizontalAlignment="Left" Margin="2" VerticalAlignment="Bottom">
                <TextBlock  FontSize="28" Margin="0 0 0 -3" Text="{Binding MonthNumber}"/>
                <TextBlock FontSize="12" FontWeight="Light" Text="{Binding MonthName}"/>
            </StackPanel>
        </Grid>
    </DataTemplate>
    <DataTemplate x:Key="Default">

        <StackPanel HorizontalAlignment="Left" Margin="2" VerticalAlignment="Bottom">
            <TextBlock  FontSize="28" Margin="0 0 0 -3" Text="{Binding MonthNumber}"/>
            <TextBlock FontSize="12" FontWeight="Light" Text="{Binding MonthName}"/>
        </StackPanel>
    </DataTemplate>
</ResourceDictionary>