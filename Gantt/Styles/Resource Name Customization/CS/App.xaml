<Application x:Class="GanttProperties.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             StartupUri="MainWindow.xaml">
    <Application.Resources>
        <DataTemplate x:Key="temp">
            <StackPanel Orientation="Horizontal">
                <Image Source="..\..\Image\Peter.png" />
                <TextBlock Width="100"
                           HorizontalAlignment="Left"
                           VerticalAlignment="Center"
                           FontFamily="Segoe UI"
                           FontStyle="Italic"
                           FontWeight="Bold"
                           Foreground="#333333"
                           Text="{TemplateBinding Content}" />
            </StackPanel>
        </DataTemplate>
        <DataTemplate x:Key="temp1">
            <StackPanel Orientation="Horizontal">
                <Image Source="..\..\Image\John.png" />
                <TextBlock Width="100"
                           HorizontalAlignment="Left"
                           VerticalAlignment="Center"
                           FontStyle="Italic"
                           FontWeight="Bold"
                           Foreground="#333333"
                           Text="{TemplateBinding Content}" />
            </StackPanel>
        </DataTemplate>
        <DataTemplate x:Key="temp2">
            <StackPanel Orientation="Horizontal">
                <Image Source="..\..\Image\Leslie.png" />
                <TextBlock Width="100"
                           HorizontalAlignment="Left"
                           VerticalAlignment="Center"
                           FontStyle="Italic"
                           FontWeight="Bold"
                           Foreground="#333333"
                           Text="{TemplateBinding Content}" />
            </StackPanel>
        </DataTemplate>
        <DataTemplate x:Key="temp3">
            <StackPanel Orientation="Horizontal">
                <Image Source="..\..\Image\Neil.png" />
                <TextBlock Width="100"
                           HorizontalAlignment="Left"
                           VerticalAlignment="Center"
                           FontStyle="Italic"
                           FontWeight="Bold"
                           Foreground="#333333"
                           Text="{TemplateBinding Content}" />
            </StackPanel>
        </DataTemplate>
        <DataTemplate x:Key="temp4">
            <StackPanel Orientation="Horizontal">
                <Image Source="..\..\Image\David.png" />
                <TextBlock Width="100"
                           HorizontalAlignment="Left"
                           VerticalAlignment="Center"
                           FontStyle="Italic"
                           FontWeight="Bold"
                           Foreground="#333333"
                           Text="{TemplateBinding Content}" />
            </StackPanel>
        </DataTemplate>
        <DataTemplate x:Key="temp5">
            <StackPanel Orientation="Horizontal">
                <Image Source="..\..\Image\Thomas.png" />
                <TextBlock Width="100"
                           HorizontalAlignment="Left"
                           VerticalAlignment="Center"
                           FontStyle="Italic"
                           FontWeight="Bold"
                           Foreground="#333333"
                           Text="{TemplateBinding Content}" />
            </StackPanel>
        </DataTemplate>
        <DataTemplate x:Key="temp6">
            <StackPanel Orientation="Horizontal">
                <Image Source="..\..\Image\Johnson.png" />
                <TextBlock Width="100"
                           HorizontalAlignment="Left"
                           VerticalAlignment="Center"
                           FontStyle="Italic"
                           FontWeight="Bold"
                           Foreground="#333333"
                           Text="{TemplateBinding Content}" />
            </StackPanel>
        </DataTemplate>
        <DataTemplate x:Key="temp7">
            <StackPanel Orientation="Horizontal">
                <Image Source="..\..\Image\Peterson.png" />
                <TextBlock Width="100"
                           HorizontalAlignment="Left"
                           VerticalAlignment="Center"
                           FontStyle="Italic"
                           FontWeight="Bold"
                           Foreground="#333333"
                           Text="{TemplateBinding Content}" />
            </StackPanel>
        </DataTemplate>
    </Application.Resources>
</Application>
