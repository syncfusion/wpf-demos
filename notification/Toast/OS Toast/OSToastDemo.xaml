<democommon:DemoControl
    x:Class="syncfusion.notificationdemos.wpf.OSToastDemo"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
    xmlns:democommon="clr-namespace:syncfusion.demoscommon.wpf;assembly=syncfusion.demoscommon.wpf"
    xmlns:local="clr-namespace:syncfusion.notificationdemos.wpf"
    mc:Ignorable="d" 
    d:DesignHeight="450" d:DesignWidth="800">




    <Grid Margin="20">
        <Border Padding="16" CornerRadius="8" BorderThickness="1">
            <!-- Use Grid for structure and row spacing instead of StackPanel.Spacing -->
            <Grid Width="360" HorizontalAlignment="Center" VerticalAlignment="Center">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0"
                           Text="Monthly Sales Report"
                           FontSize="16"
                           FontWeight="SemiBold"
                           Margin="0,0,0,8"/>

                <TextBlock Grid.Row="1"
                           Text="Click Download to export the report. A toast will appear when it’s ready."
                           TextWrapping="Wrap"
                           Margin="0,0,0,12"/>

                <!-- Action row -->
                <Grid Grid.Row="2" Margin="0,0,0,10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>

                    <Button x:Name="DownloadButton"
                            Grid.Column="0"
                            Content="Download report"
                            Width="160"
                            Click="OnStartDownload"
                            Margin="0,0,8,0"/>

                    <TextBlock x:Name="StatusText"
                               Grid.Column="1"
                               VerticalAlignment="Center"
                               Text=""/>
                </Grid>

                <ProgressBar Grid.Row="3"
                             x:Name="DownloadBar"
                             Height="10"
                             Minimum="0"
                             Maximum="100"
                             Value="0"/>
            </Grid>
        </Border>
    </Grid>



</democommon:DemoControl>
