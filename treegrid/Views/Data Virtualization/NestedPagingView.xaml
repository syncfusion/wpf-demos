<Page
    x:Class="syncfusion.treegriddemos.wpf.NestedPagingView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="clr-namespace:syncfusion.treegriddemos.wpf"
    xmlns:datapager="clr-namespace:Syncfusion.UI.Xaml.Controls.DataPager;assembly=Syncfusion.SfGrid.WPF"
    xmlns:syncfusionskin="clr-namespace:Syncfusion.SfSkinManager;assembly=Syncfusion.SfSkinManager.WPF"
    syncfusionskin:SfSkinManager.Theme="{syncfusionskin:SkinManagerExtension ThemeName=Windows11Light}"
    Background="#FFFFFF"
    xmlns:syncfusion="http://schemas.syncfusion.com/wpf" >

    <Page.DataContext>
        <local:PagingViewModel />
    </Page.DataContext>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>

        <syncfusion:SfTreeGrid x:Name="treeGrid"
                               Margin="5"
                               AutoExpandMode="AllNodesExpanded"
                               AutoGenerateColumns="False"
                               ChildPropertyName="Children"
                               ColumnSizer="Star"
                               ItemsSource="{Binding ElementName=sfDataPager, Path=PagedSource}" >
            <syncfusion:SfTreeGrid.Columns>
                <syncfusion:TreeGridTextColumn HeaderText="First Name" MappingName="FirstName" />
                <syncfusion:TreeGridTextColumn HeaderText="Last Name" MappingName="LastName" />
                <syncfusion:TreeGridNumericColumn HeaderText="ID" MappingName="ID" NumberDecimalDigits="0" />
                <syncfusion:TreeGridTextColumn HeaderText="Title" MappingName="Title" />
                <syncfusion:TreeGridNumericColumn MappingName="Salary" HeaderText="Salary" NumberDecimalDigits="0" />
            </syncfusion:SfTreeGrid.Columns>
        </syncfusion:SfTreeGrid>

        <datapager:SfDataPager x:Name="sfDataPager"
                               Grid.Row="1"
                               Grid.Column="0"
                               Margin="5"
                               HorizontalAlignment="Center"
                               VerticalAlignment="Center"
                               NumericButtonCount="6"
                               Orientation="Horizontal"
                               PageSize="3"
                               Source="{Binding PersonDetails}" />
    </Grid>
</Page>